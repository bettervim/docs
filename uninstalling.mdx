If you want to uninstall Better Vim, run this command on your terminal:

```bash
curl -L https://bettervim.com/uninstall | bash
```

If you already had a neovim configuration before installing Better Vim, a backup was created, and we'll recover it after run the uninstalling script.
